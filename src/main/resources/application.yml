spring:
  application:
    name: staj

  datasource:
    url: jdbc:postgresql://postgres:5432/staj
    username: postgres
    password: 12345

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8081